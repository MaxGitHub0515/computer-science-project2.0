services:
  frontend:
    build: ./client
    ports:
      - "3500:3000"
    depends_on:
      - backend
    networks:
      - app-network

  backend:
    build: ./server
    ports:
      - "8001:8001"
    environment:
      - PORT=8001
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
